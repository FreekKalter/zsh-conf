#!/bin/sh

git clone https://github.com/robbyrussell/oh-my-zsh.git ~/.oh-my-zsh

ln -fs .zshrc ~/.zshrc
ln -fs .zsh_aliases ~/.zsh_aliases
ln -fs .tmux.conf ~/.tmux.conf
ln -fs freek.zsh-theme ~/.oh-my-zsh/themes/freek.zsh-theme
