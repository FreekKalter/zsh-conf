alias rc='killall conky && conky -dq'
alias sab='python /home/fkalter/SABnzbd/SABnzbd.py >> /home/fkalter/SABnzbd/sab.log 2>&1 &'

alias rms='find . -name ".*.sw[a-z]" -print -delete'
alias unrarall='find . -name "*.rar" -exec unrar e -ad {} \;'

# tunnel (when using internet op public wifi)
# -v for verbose so dedicate a terminal to it
# -N for not execting a command on the host
# -D portnumber for local forward (set this in firefox)
alias london_tunnel='ssh -vN -D 10000 london'
alias awk='gawk --re-interval'

# git shortcuts
alias gs='git status'
alias glog='git log --decorate --graph --oneline -5'
alias v='vim'
alias gd='git diff'

# coloring
alias grep='grep --color=auto'
alias fgrep='fgrep --color=auto'
alias egrep='egrep --color=auto'

# commands i use alot
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
alias xx='exit'
alias s='sudo'

# global aliases
alias -g L="| less"
alias -g sus="echo '~/scripts/suspend'"

# Add an "alert" alias for long running commands.  Use like so:
#   sleep 10; alert
alias alert='notify-send --urgency=low -i "$([ $? = 0 ] && echo terminal || echo error)" "$(history|tail -n1|sed -e '\''s/^\s*[0-9]\+\s*//;s/[;&|]\s*alert$//'\'')"'
alias tmux='TERM=screen-256color-bce tmux'

# command to find wich commands i use alot
function fmuc()
{
    cat ~/.zsh_history | perl -ne 's/^.*;//;$h{$_}++; END{@s=sort{$h{$b}<=>$h{$a}}keys %h;for($i=0;$i<10;$i++){print $s[$i];}}'
}
